TAAL INDIAN KITCHEN & BAR — Website Documentation
===================================================
Last updated: 2026-06-16

FILE STRUCTURE
--------------
index.html          Main page (single-page layout with anchor sections)
css/styles.css      All layout, component, and responsive styles
css/animations.css  Keyframes, scroll-reveal, page loader, and motion styles
js/main.js          Nav behavior, ambient audio toggle, scroll reveals, parallax, forms
js/menu.js          Menu tab switching, dietary filtering, spice tooltips
Branding/           Logo images and hero video (see below)

BRANDING ASSETS
---------------
Branding/PHOTO-2026-06-15-16-35-54.jpg    Primary logo (dark green bg) — used in hero
Branding/PHOTO-2026-06-15-16-35-54_1.jpg  Brand sheet — reference only
Branding/PHOTO-2026-06-15-16-35-54_2.jpg  Coming-soon flyer (used as video poster)
Branding/PHOTO-2026-06-15-16-35-54_3.jpg  Coming-soon flyer v2 — used in footer
Branding/PHOTO-2026-06-15-16-35-54_4.jpg  Circle logo (white bg) — reference
Branding/VIDEO-2026-06-15-16-35-54.mp4    Hero video — autoplay muted loop

PLACEHOLDER: assets/audio/ambient-sitar.mp3
  → Add real ambient tabla/sitar MP3 here. Audio toggle in nav is wired up and ready.

FEATURES IMPLEMENTED
---------------------

NAVIGATION
  - Fixed transparent nav that turns solid dark green on scroll
  - Animated SVG sound wave / bansuri motif logo mark
  - Cinzel font for "TAAL" wordmark, Montserrat for "Indian Kitchen & Bar"
  - Desktop: dropdown menus for Menu and Events
  - Dropdown Menu links jump to the correct menu tab on click
  - Mobile: hamburger menu with slide-down overlay
  - "Reserve a Table" gold CTA button in nav
  - Active link highlight as user scrolls through sections
  - Keyboard accessible: Enter/Space open dropdowns, Escape closes them
  - Smooth scroll to all anchor links with nav-height offset

HERO SECTION
  - Autoplay muted looping hero video (Branding/VIDEO-2026-06-15-16-35-54.mp4)
  - Dark green overlay for readability
  - Hero logo image (dark green bg variant) centered over video
  - Neighborhood callout: "Chapel Hill's only upscale Indian dining experience"
  - Gold divider with diamond separator
  - Italic tagline: "Inspired by tradition. Crafted for today."
  - Two CTAs: "Reserve Your Table" (gold) and "Explore the Menu" (outline)
  - Animated peacock feather SVG — left side, floating animation
  - Scroll indicator with animated bar
  - Parallax: hero video translates on scroll (30% rate)
  - All hero elements have staggered CSS entrance animations

AMBIENT AUDIO
  - Toggle button in nav (speaker icon)
  - Plays/pauses assets/audio/ambient-sitar.mp3 (placeholder path — add file)
  - Active state: ring pulse animation on button
  - Nav logo SVG sound wave bars animate when audio is playing
  - Handles autoplay blocked gracefully (no console errors)

PAGE LOADER
  - Animated gold sound wave bars + "TAAL" text
  - Fades out after window load + 400ms delay

SCROLL PROGRESS BAR
  - 2px gold gradient bar at very top of page, tracks scroll position

CALLOUT BANNER
  - Full-width dark green banner below hero
  - "Chapel Hill's first and only upscale contemporary Indian dining experience"

ABOUT SECTION
  - Two-column layout: image placeholder (left) + content (right)
  - Brand story copy with music/rhythm language ("notes of cardamom", etc.)
  - Three icon pillars: Exquisite Cuisine, Thoughtful Details, Timeless Experiences
  - SVG flute motif used in pillar icons
  - Scroll-reveal animations: left/right entrance

MENU SECTION
  - Dark green background matching brand
  - Seven tabs: Dinner, Lunch, Biryanis, Cocktails, Beverages, Beer & Wine, Desserts
  - Dietary filter buttons: All / Vegetarian / Vegan / Gluten-Free
  - Filter operates per active tab only
  - Animated menu item entrance (Intersection Observer stagger)
  - Mobile: tap menu item to expand/collapse description
  - Spice level indicators: SVG flame icons (not emoji peppers)
  - Dietary badges: 🌿 Vegetarian, V Vegan, GF Gluten-Free
  - Food photo placeholder boxes (easy to replace with real images)
  - Compact layout for sides, cocktails, beer & wine
  - Seasonal note + dietary key at bottom
  - Nav desktop dropdown and mobile sub-links include all 7 tabs

MENU CONTENT (placeholder — replace when real menu is ready)
  Dinner:     Chef's Specials (3 items: Tasting Menu $95, Weekend Special $68, Seasonal Thali $38),
              Small Plates (4), Mains (4), Breads & Sides (3)
  Lunch:      Lunch Specials (3 items: Daily Biryani Special $19, Lunch Tasting $42, Market Sabzi $17),
              Small Plates (3), Mains (2 business lunch options)
  Biryanis:   6 biryanis (Hyderabadi Dum $32, Gosht $36, Prawn & Coconut $34, Heirloom Veg $26,
              TAAL Signature $38) + Accompaniments (Mirchi ka Salan $6, Boondi Raita $5, Pickle $4)
  Cocktails:  6 cocktails + 2 lassis
  Beverages:  Indian Teas & Coffees (4), Fresh Pressed & Coolers (4), Lassi Bar (3),
              Still & Sparkling (3)
  Beer/Wine:  4 wine categories + 3 beers
  Desserts:   3 desserts

EVENTS / CORPORATE SECTION
  - Three cards: Corporate Dining, Private Events, Catering
  - Each has SVG icon, headline, italic sub, description, feature list, CTA
  - Hover: lift + border glow animation
  - Corporate callout: "20 minutes from Research Triangle Park"
  - Catering: UNC/Duke/NC State + 20-500 guests capacity

GALLERY SECTION
  - Masonry-style grid (3 cols, some wide items spanning 2 cols)
  - 6 placeholder slots with labels (easy to swap with real photos)
  - Hover: dark overlay + subtle scale

RESERVATIONS SECTION
  - Dark green bg, two columns
  - Left: hours info (Dinner/Lunch/Bar), walk-in note
  - Right: reservation form (name, email, date, time, party size, notes)
  - Date input pre-set to today's date with minimum = today
  - INTEGRATION NOTE in HTML: replace form with Resy or OpenTable embed

CONTACT SECTION
  - Light bg, two columns
  - Left: address, Instagram, website, email, hours grid
  - Right: map placeholder with location pin SVG + "Get Directions" link,
           contact form (name, email, subject dropdown, message)
  - Contact form submits with simulated success state (2-second delay)

FOOTER
  - Dark background (#120800)
  - Four columns: Brand (logo + tagline + Instagram), Explore links, Menu links, Newsletter
  - Footer logo = coming-soon flyer image (light version)
  - Animated SVG wave motif in footer bottom
  - Newsletter form (email + arrow submit button)
  - Copyright, Privacy Policy, Terms, Accessibility links

SEO
  - Title: "TAAL Indian Kitchen & Bar | The Rhythm of Indian Dining | Chapel Hill, NC"
  - Meta description targeting upscale Indian, Chapel Hill, Triangle NC
  - Keywords: "best Indian restaurant Chapel Hill", "upscale Indian Triangle NC", "private dining Chapel Hill"
  - Schema.org Restaurant structured data (address, hours, cuisine, price range)
  - Open Graph tags for social sharing
  - Canonical URL set to taaldining.com
  - rel="noopener noreferrer" on all external links

ACCESSIBILITY
  - All images have descriptive alt text
  - ARIA labels on interactive elements
  - role="tablist" / role="tab" / role="tabpanel" on menu tabs
  - role="navigation", role="contentinfo", role="dialog" on key elements
  - aria-hidden on decorative SVGs and icons
  - prefers-reduced-motion media query disables all animations
  - Keyboard-accessible dropdowns (Enter/Space/Escape)

DESIGN SYSTEM
  Fonts: Cinzel (display), Cormorant Garamond (headings/body), Great Vibes (cursive accents), Montserrat (UI/nav), Lato (body text)
  Colors:
    --green-dark:  #0A2B1E  (primary bg)
    --green-mid:   #0F3D2B  (events section bg)
    --gold:        #C9A227  (primary accent)
    --gold-light:  #E8C96A  (hover / light gold)
    --cream:       #F8F3E8  (light text / bg)
    --off-white:   #FAF7F2  (light section bg)
    --dark:        #120800  (footer bg)

ANIMATIONS
  - CSS keyframes: fadeIn, fadeUp, fadeLeft, fadeRight, scaleIn, logoReveal
  - featherFloat: peacock feather gentle sway
  - waveBar1â€“5: sound wave bar bounce (loader + audio-playing logo)
  - scrollPulse: hero scroll indicator
  - pulseRing: audio toggle active state ring
  - drawLine: footer SVG wave entrance
  - Page loader: animated wave bars + logo fade
  - Scroll reveal: IntersectionObserver drives .reveal-up / .reveal-left / .reveal-right
  - Stagger utility: .stagger class delays children 0â€“500ms

RESPONSIVE
  - Breakpoint 1100px: events grid → single column, footer → 2 columns
  - Breakpoint 900px: about → single column, nav → hamburger only, footer → single column
  - Breakpoint 640px: hero CTA → stacked, menu items → single column, gallery → single column

CHANGES LOG
------------
2026-06-16f: Feature + two bug fixes:
            FEATURE — Instrument line dividers replacing spice dividers:
               The four animated spice dividers (large cream-background blocks with cardamom/saffron/lotus
               SVG icons that were invisible — gold icons on cream background) were removed and replaced
               with slim, elegant horizontal line dividers featuring hand-drawn SVG Indian instrument
               silhouettes. Each divider is a 1.25rem-tall horizontal rule with a centered instrument icon
               and gradient lines extending to each side. Four unique instruments, one per section transition:
               1. Tabla (top-down view of bayan + dayan drum pair) — About → TAAL Meaning (light bg)
               2. Sitar (neck with frets, gourd body, tuning pegs) — TAAL Meaning → Menu (dark bg)
               3. Bansuri (horizontal bamboo flute with embouchure hole + 6 finger holes) — Events → Gallery (mid bg)
               4. Veena (long neck, secondary upper gourd, main resonator) — Gallery → Reservations (light bg)
               CSS: .spice-divider block removed; .instrument-divider, .instrument-divider--light,
               .instrument-divider--dark, .instrument-divider--mid added to styles.css.
               The old .spice-divider CSS block has been fully replaced.

            BUG FIX 1 — Emoji/symbol mojibake in index.html:
               Multiple emoji and Unicode symbols were stored as double-encoded UTF-8 mojibake (UTF-8 bytes
               misread as Windows-1252, then re-encoded). Fixed all instances:
               🌿 (herb/vegetarian badge, 17 instances) was ðŸŒ¿
               🍽 (About section placeholder icon, 1 instance) was ðŸ½
               ♩ (callout banner musical note, 2 instances) was â™©
               → (newsletter subscribe button arrow, 1 instance) was â†'
               📍 (contact address pin icon, 1 instance) was ðŸ"
               📸 (contact Instagram camera icon, 1 instance) was ðŸ"¸
               🌐 (contact website globe icon, 1 instance) was ðŸŒ
               ✉️ (contact events email envelope, 1 instance) was âœ‰ï¸

            BUG FIX 2 — Diamond bullet mojibake in styles.css:
               .event-features li::before { content: 'â—†' } was rendering mojibake instead of ◆ (U+25C6).
               The CSS content property value was double-encoded. Fixed to correct Unicode character.

2026-06-16e: Two bug fixes in index.html:
            1. Nav arrow mojibake: "Menu ›" and "Events ›" nav dropdown arrows were stored as â€º (mojibake
               for U+203A, single right angle quotation mark). Fixed.
               Additional en dash ranges (Fri–Sun, Wed–Sun, 8–40, etc.) were stored as â€" (mojibake for
               U+2013, en dash — different from the em dash fix in 2026-06-16c). Fixed.
               Textarea placeholder ellipsis (…) was stored as â€¦ (mojibake for U+2026). Fixed.
            2. Section dividers — parallax green bars replaced with spice dividers:
               The two "Parallax Food Photography Bands" (320px tall dark green gradient bars) between
               About→TAAL Meaning and Gallery→Reservations were displaying as plain green bars with no
               photos, obscuring the animated spice divider aesthetic. Both parallax bands removed and
               replaced with animated spice dividers (matching the existing one between Events and Gallery).
               A new spice divider was also added between the TAAL Meaning and Menu sections where none
               existed. Total spice dividers: 4 (About→TAAL Meaning, TAAL Meaning→Menu, Events→Gallery,
               Gallery→Reservations). Parallax band CSS (.parallax-band, .parallax-inner) remains in
               styles.css but is now unused — can be cleaned up when real food photos are added.

2026-06-16c: Two bug fixes — encoding/character corruption in index.html:
            1. Section dividers: All 12 HTML section-divider comments had their ═ (U+2550) box-drawing
               characters stored as mojibake (UTF-8 bytes reinterpreted as Windows-1252). Fixed by replacing
               all instances with correct characters. Two PARALLAX section comment blocks also had duplicate
               <!-- openers from a partial prior fix, and the orphaned section headers ("THE NAME — TAAL
               MEANING" and "RESERVATIONS") were rendering as visible text on the page outside any HTML
               comment. Fixed the comment structure for both blocks.
            2. Devanagari and special characters: "ताल" (Taal in Devanagari script) plus em dashes (—),
               middle dots (·), and the black diamond (◆) were all stored as double-encoded UTF-8 mojibake.
               All instances corrected in index.html.

2026-06-16b: Bug fix — spice divider SVGs rendered as garbled text.
            Root cause: the gallery section's 3-line HTML comment (<!-- ═══ / GALLERY / ═══ -->) was
            split by the spice divider insertion. The opening <!-- stayed above the spice divider;
            the closing --> and body text ("GALLERY", garbled box-drawing characters) landed below it.
            In HTML5 the --> inside "<!-- Animated spice divider -->" closed the outer comment, so the
            spice SVGs rendered correctly — but the stray "GALLERY" text and garbled ═══ characters on
            the lines below them were exposed as visible page text between the spice divider and the
            gallery section. Fixed by deleting the three orphaned comment lines from index.html.

2026-06-16a: Scope correction — reverted 13 unauthorized features added in 2026-06-15m that were not
            in Changes.md, and implemented the 4 legitimate Changes.md features:
            REVERTED (removed from HTML/CSS/JS):
              Ingredient Spotlight (#ingredient-spotlight section + carousel JS)
              Cocktail Spotlight (#cocktail-spotlight section)
              Happy Hour Banner (#happy-hour section)
              Chef Bio Section (#chef section + nav link)
              Press & Awards (#press section + carousel JS)
              Testimonials (#testimonials section + carousel JS)
              Instagram Feed (#instagram section)
              FAQ Section (#faq section + accordion JS + nav link)
              Allergen Info Toggles (allergen-wrap divs + JS)
              Drink Pairings (item-pairing paragraphs)
              Active Tab Background CSS (removed the tab gradient override)
              Mailchimp newsletter form (reverted to simple email+arrow form)
              Tidio live chat comment (removed)
            IMPLEMENTED (from Changes.md):
              1. Parallax Food Photography Bands: two full-width decorative parallax bands added —
                 one before #taal-meaning (caption "The Composition"), one before #reservations
                 (caption "The Table"). CSS gradient patterns with background-attachment:fixed.
              2. Animated Spice Divider: three SVG icons (cardamom ellipse, saffron stigma strands,
                 lotus petals) with gold strokes, floating on a spice-float @keyframes animation,
                 separated by gradient lines. Placed between the Events and Gallery sections.
              3. Menu Tab Photo Backgrounds: 10 data-bg attributes added to menu tabs (spice, curry,
                 biryani, herb, honey, citrus, grain, petal, grain2, malt). Each tab's ::before
                 pseudo-element shows a unique subtle gradient on hover and when active.
              4. QR Code Menu Page: new file menu.html — standalone printable page with TAAL-branded
                 header, CSS-generated QR pattern placeholder (replace with real QR image), numbered
                 print instructions, Print button, and print CSS that hides instructions. Links back
                 to index.html. Intended for table tent cards.

2026-06-15m: Major feature batch — 20 features added from the Website Ideas list:
            1. Dark/Light Mode Toggle: moon/sun icon button added to nav. Defaults to dark. Preference saved in localStorage. Light mode inverts hero, nav, menu, events, and reservations section backgrounds to cream/off-white while keeping footer dark. CSS [data-theme="light"] overrides.
            2. Back-to-Top Button: floating gold circle button bottom-right, appears after 400px scroll, smooth scrolls to top.
            3. Google Maps embed: replaced SVG map placeholder in contact section with a real Google Maps iframe (maps.google.com embed URL for 310 W Franklin St).
            4. Occasion Selector: added "Occasion" dropdown to reservation form (Birthday, Anniversary, Business Dinner, Date Night, Proposal, Graduation, Other).
            5. FAQ Section: new section (#faq) before footer with 10 Q&As in gold accordion. Only one item open at a time.
            6. Taal Meaning Section: new dark green section (#taal-meaning) after About explaining "taal" as the rhythmic cycle in Indian classical music. Decorative circular tabla SVG.
            7. Ingredient Spotlight: new cream section (#ingredient-spotlight) after Taal Meaning. Auto-rotating 5s carousel across Cardamom, Saffron, and Tamarind with SVG icons, origin notes, and how TAAL uses each.
            8. Cocktail Spotlight: new dark green section (#cocktail-spotlight) after Menu. Features The Peacock ($18) and Rhythm of Rosewater ($16) with photo placeholders and pairing suggestions.
            9. Happy Hour Banner: slim banner (#happy-hour) after Cocktail Spotlight — "Tuesdayâ€“Friday 5â€“7 PM · 20% off cocktails & wine."
            10. Chef Bio Section: new cream section (#chef) with placeholder portrait, bio for Chef Arjun Mehta, Great Vibes cursive signature.
            11. Press & Awards Section: new cream section (#press) after Gallery. Five press logo placeholder badges + auto-rotating 4s quote carousel with dots navigation.
            12. Testimonials Section: new cream section (#testimonials) after Press. Five guest quotes in auto-rotating 4.5s carousel with dot navigation.
            13. Instagram Feed Section: new section (#instagram) after Testimonials. 3×2 grid of placeholder tiles with Instagram SVG icons. Integration comment for Elfsight/SnapWidget.
            14. Inline Event Inquiry Forms: replaced "Enquire Now / Plan Your Event / Get a Quote" anchor links on all 3 event cards with toggle buttons that expand tailored inline mini-forms. Each form submits with the same 1.2s simulated success state.
            15. Allergen Info Toggles: added collapsible allergen tag lists below 13 specific menu items (Paneer Tikka, Lamb Seekh Kebab, Heirloom Beet Chaat, Scallop Amritsari, Raan-e-TAAL, Shahi Paneer Royale, Wild Salmon Malabar, Dal TAAL, 5 Biryanis, 3 Desserts).
            16. Drink Pairings: added "Pairs with →" callouts (gold, subtle) to 6 menu items linking to their cocktail match (Paneer Tikka→Rhythm of Rosewater, Lamb Seekh→Saffron Negroni, Raan-e-TAAL→Mango Taal, Shahi Paneer→Rose & Pistachio Lassi, Hyderabadi Biryani→Masala Mule, Gosht Biryani→Saffron Negroni).
            17. Active Menu Tab Background: active .menu-tab now has a subtle gold gradient background in addition to the gold border.
            18. Newsletter Mailchimp Structure: updated footer newsletter form to standard Mailchimp POST action format with NAME field and honeypot. XXXX placeholders for list/user IDs.
            19. 404.html: created branded error page with large "404" in gold Cinzel, "The table you're looking for doesn't exist." in Cormorant italic, and a Return to Home button.
            20. Live Chat Widget: Tidio integration comment added before </body> in index.html.
            Also added: Chef and FAQ to desktop nav links and mobile nav menu.

2026-06-15i: Comprehensive mobile responsiveness pass (375â€“640px):
            Container & nav padding reduced to 1.25rem on mobile.
            Section vertical padding reduced from 7rem to 4rem on mobile.
            Menu tabs switch to horizontal-scroll single row (no wrap) with hidden scrollbar — all 10 tabs accessible by swiping.
            Nav hamburger and audio toggle enlarged to 44×44px touch targets.
            Filter buttons min-height 36px. Form inputs font-size 1rem (prevents iOS auto-zoom on focus).
            Form labels increased to 0.75rem for readability. Form inputs padding increased to 0.85rem.
            Newsletter form stacks vertically on mobile.
            Hero peacock feather hidden on mobile (declutter). Events grid full-width on mobile.
            Footer top padding reduced. All existing 640px/900px/1100px breakpoints preserved.
2026-06-15l: Three bug fixes:
            1. Ambient audio volume reduced to 30% (audio.volume = 0.3) so the sitar background is subtle.
            2. Scroll indicator still overlapping buttons: reduced hero logo from clamp(220px,36vw,400px) to clamp(180px,28vw,340px), increased hero min-height from 620px to 700px, and increased scroll indicator margin-top to 3rem. This ensures hero-content always fits within the hero viewport without overflow, keeping the scroll indicator visually clear of both CTA buttons.
            3. Animation lag on mobile: disabled featherFloat on mobile (just fadeIn), disabled scrollPulse on mobile (static bar), stopped wave-bar animations on mobile (8 concurrent animations), removed filter:blur from logoReveal on mobile (blur is GPU-expensive), shortened scroll-reveal transitions to 0.45s on mobile, added will-change hints to continuously looping elements.
2026-06-15k: Two remaining mobile/hero bug fixes (second attempt with correct root causes):
            1. Nav drawer still scrolled background on iOS: replaced document.body.style.overflow='hidden' (which iOS ignores) with the position-fixed body-freeze technique: saves scrollY, sets body to position:fixed+top:-scrollY+width:100% on open, restores on close. Also added overscroll-behavior:contain to .nav-mobile.open CSS.
            2. Scroll animation overlapping "Explore the Menu" button (both desktop+mobile): root cause was that .btn-secondary has background:transparent, so the absolutely-positioned gold scroll line at left:50% showed through the button at all z-index levels. Fix: moved .hero-scroll div from being a sibling of .hero-content (absolutely positioned at bottom:2.5rem) to being the last child inside .hero-content (normal document flow, after the CTA). Changed CSS from position:absolute to margin-top:2.5rem + pointer-events:none. Now the scroll indicator is always visually below the buttons, guaranteed by DOM order.
2026-06-15j: Three mobile bug fixes:
            1. Audio mute button definitively fixed on mobile: moved the audio toggle button out of .nav-cta in HTML so it is a direct sibling in .nav-container. .nav-cta (reserve button) can now be hidden at 900px as before without affecting the audio toggle. Added margin-left and flex-shrink:0 to .audio-toggle so desktop spacing is preserved.
            2. Mobile nav scroll bleed fixed: added max-height: calc(100vh - nav-height) and overflow-y: auto to .nav-mobile.open so the menu scrolls within itself instead of letting touch events reach the background page.
            3. Hero "scroll" animation overlapping "Explore the Menu" button fixed: lowered .hero-scroll z-index from 2 to 1 so .hero-content (z-index 2) always renders in front of it.
2026-06-15i: Fixed audio mute button not appearing on mobile. The .nav-cta container was hidden entirely at the 900px breakpoint, taking the audio toggle with it. Changed the rule to only hide .btn-reserve (which already appears in the mobile slide-out menu), leaving .nav-cta visible so the audio toggle remains accessible in the mobile nav bar beside the hamburger.
2026-06-15h: Fixed ambient audio autoplay. Attempts direct play on window load; if browser blocks it, starts on first user interaction (scroll/click/touch/keydown). Mute button icon now shows speaker-waves when playing and slashed-speaker when muted. Audio starts playing by default.
2026-06-15g: Audio autoplay attempted on load + first-interaction fallback. Icon logic corrected: speaker icon = currently playing (click to mute), muted icon = currently muted (click to play).
2026-06-15f: Reordered menu tabs to: Small Plates, Mains, Biryanis, Sides & Breads, Desserts, Beverages, Beer & Wine, Cocktails, Lunch, Dinner.
            Small Plates is now the default active tab on page load.
            Desktop nav dropdown and mobile sub-links updated to match new order.
2026-06-15e: Restructured menu: Small Plates, Mains, Sides & Breads promoted to standalone all-day tabs.
            Dinner tab now shows Chef's Specials only. Lunch tab shows Lunch Specials only.
            Removed Lunch Plates and Business Lunch sections (superseded by all-day menu tabs).
            Desktop nav dropdown and mobile sub-links updated with all 10 tabs.
2026-06-15d: Expanded menu from 5 tabs to 7 tabs: added Biryanis and Beverages (Non-Alcoholic).
            Dinner tab: added Chef's Specials category at top (Tasting Menu $95, Weekend Special $68, Seasonal Vegetable Thali $38).
            Lunch tab: added Lunch Specials category at top (Daily Biryani Special $19, Lunch Tasting $42, Market Sabzi Special $17).
            Biryanis tab: 6 biryanis + Accompaniments sub-section.
            Beverages tab: 4 categories — Indian Teas & Coffees, Fresh Pressed & Coolers, Lassi Bar, Still & Sparkling.
            Updated desktop nav dropdown and mobile sub-links to include Biryanis and Beverages.
2026-06-15c: Removed video from page loader entirely. Loader is now dark green bg with animated wave bars + TAAL wordmark only.
            Loader dismiss timing reverted to 400ms after window load.
            Added favicon: Branding/PHOTO-2026-06-15-16-35-54.jpg (also set as apple-touch-icon).
            Hero logo image uses mix-blend-mode:screen — dark green box background disappears into gradient, gold logo floats cleanly.
2026-06-15: Moved hero video from blurry background to page loader animation.
            Hero now uses a dark green CSS gradient background (#061711 → #0A2B1E → #0d3525).
            Loader plays video fullscreen with dark overlay + TAAL wordmark + wave bars on top.
            Loader stays visible for minimum 3.5 seconds so video has time to show.
            Removed hero parallax (no longer relevant without video element).
            Added subtle decorative gold wave SVGs to hero background.
            Added radial gold glow behind hero logo area.

THINGS TO ADD LATER
--------------------
  - Real menu photos (replace .img-placeholder divs with <img> tags)
  - Real gallery photos (replace .gallery-placeholder divs with <img> tags)
  - Resy or OpenTable embed (replace reservation form in #reservations)
  - Ambient sitar/tabla MP3 at assets/audio/ambient-sitar.mp3
  - Real restaurant interior photos for about section
  - Phone number (add to contact section)
  - Real email setup for contact/events forms (connect to backend or Formspree)
  - Newsletter email service connection (Mailchimp, etc.)
  - Google Analytics or similar tracking
  - Google Maps embed (replace .map-placeholder)
  - Favicon / apple-touch-icon
